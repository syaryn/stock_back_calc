pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.{ts,tsx,js,json}"
      run: deno fmt --check {staged_files}
    lint:
      glob: "*.{ts,tsx,js}"
      run: deno lint {staged_files}
