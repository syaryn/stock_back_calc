{
  "tasks": {
    "dev": "deno serve --host 127.0.0.1 -A --watch main.tsx",
    "check": "deno task lint && deno task fmt && deno task test && deno audit",
    "test": "deno test --allow-read --allow-env --allow-net",
    "lint": "deno lint",
    "fmt": "deno fmt"
  },
  "imports": {
    "hono": "jsr:@hono/hono@^4.0.0",
    "@std/assert": "jsr:@std/assert@1"
  },
  "compilerOptions": {
    "jsx": "precompile",
    "jsxImportSource": "hono/jsx"
  }
}
