pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.ts"
      run: mise exec -- deno lint {staged_files}
    fmt:
      glob: "*.ts"
      run: mise exec -- deno fmt --check {staged_files}
